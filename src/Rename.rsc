module Rename

import AST;
import Resolve;
import IO;
import List;
import String;

set[loc] eqClass(loc n, Use use) {
  // TODO
  return {};
}

str rename(str src, loc x, str new, Refs r) {
  // TODO
  return src;
}
 

